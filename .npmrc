save-exact=true

# Toolchain phone-home, off.
# fund and update-notifier exist only to print solicitations.
# audit=false stops the *automatic* dependency-tree upload on every install;
# it does not remove the check - run `npm audit` deliberately when vetting a
# dependency, which the engineering rules still require before adoption.
fund=false
audit=false
update-notifier=false
